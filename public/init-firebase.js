// TODO: Replace the following with your app's Firebase project configuration
var firebaseConfig = {
    // ...
    databaseURL: "https://minicrm-245403.firebaseio.com/",
};
  
// Initialize Firebase
firebase.initializeApp(firebaseConfig);